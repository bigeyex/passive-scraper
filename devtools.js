chrome.devtools.panels.create(
    "Passive Scraper",
    "icon.png", // We don't have an icon yet, but it's optional or we can use a placeholder if we had one. 
    "panel/index.html",
    function (panel) {
        // code invoked on panel creation
    }
);
